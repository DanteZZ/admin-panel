<_info>
	<title>Список категорий</title>
	<template>default</template>
	
	<datajson>
		{
			"columns": [
				{
					"name":"Название",
					"key":"name"
				},
				{
					"name":"Описание",
					"key":"description"
				}
			],
			"buttons": [
				{
					"view":"text",
					"title":"Редактировать",
					"action": "href",
					"href":"./catedit?id={id}"
				},
				{
					"view":"text",
					"title":"Удалить",
					"action": "remove"
				}
			]
		}
	</datajson>

	<dataphp>
		GLOBAL $DS;
		$list = $DS->list("service_category");
		return Array("list"=>$list);
	</dataphp>

</_info>

<_template>

	<div class="bgc-white bd bdrs-3 p-20 mB-20 col-md-12">
		<h4 class="c-grey-900 mB-20">{{PAGE.title}}</h4>
		<ds-table :columns="columns" :data="list" :buttons="buttons" create_link="./catedit"></ds-table>
	</div>

</_template>

